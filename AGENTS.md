# AGENTS.md — Polemica Notes

Справочник для ИИ-агентов и людей, работающих с этим кодом. Прочитай целиком,
прежде чем менять код: половина файла — это дорого купленные уроки четырёх
больших аудитов (v8.1.21–8.1.24, июль 2026), и нарушение любого инварианта из
раздела 4 с высокой вероятностью вернёт уже починенный класс багов.

## 1. Что это

Браузерное расширение (Chrome + Firefox, Manifest V3) для polemicagame.com —
онлайн-версии спортивной мафии. Возможности: заметки об игроках со статистикой,
метки «мой read» по ролям, статистика разбора матча, автопринятие игры,
интеграция с OBS Studio (переключение сцен день/ночь, скрытие роли для стрима),
Twitch-чат поверх игры, хоткеи.

- Юзербаза маленькая, но живая; данные пользователей (заметки) — главная
  ценность, их потеря недопустима.
- `legacy/` — старая JS-версия до TypeScript-рефакторинга. НЕ собирается и НЕ
  правится; используется только как справка по прежнему поведению и форматам
  данных. Там же лежит `legacy/match_314446.json` — **настоящий JSON матча**,
  единственный источник правды о форме данных API (см. раздел 5).

## 2. Сборка и релиз

```bash
npm run typecheck        # tsc --noEmit — обязан быть чистым
npm run build            # оба таргета: dist/chrome и dist/firefox
npx web-ext lint -s dist/firefox   # 0 errors; ~23 warnings (innerHTML) — норма
```

- Один JS-бандл (tsup, iife, без минификации) на оба браузера; различия только
  в манифесте (`src/manifest/manifest.base.json` + overlay chrome/firefox,
  сборка `scripts/assemble.mjs`).
- Версию надо поднимать в ДВУХ местах: `package.json` и
  `src/manifest/manifest.base.json` (автосинка нет).
- Релиз: commit → push → zip обоих dist → `gh release create vX.Y.Z` с
  русскими release notes и обоими zip. Тегов локально нет — тег создаёт gh.
- Минимальные версии: Chrome 116 (`minimum_chrome_version`), Firefox 121
  (`strict_min_version`; поднято из-за `:has()` в инжектируемом CSS).

## 3. Архитектура

```
src/
  core/        — общий слой (дублируется в каждый бандл, состояние ПЕР-контекстное!)
    feature.ts       FeatureManager: enable/update/disable по настройкам,
                     очередь sync, откат disable() при упавшем enable()
    dom.ts           SharedDomObserver — ЕДИНСТВЕННЫЙ MutationObserver на документ
                     (childList+subtree+attributes class/style, rAF-батчинг,
                     буфер ≤4000, таймер-фолбэк в фоновой вкладке)
    settings.ts      storage.sync + LOCAL_KEYS (секреты → storage.local)
    notes-store.ts   заметки в storage.local + разовая миграция из sync
    keyboard.ts      единый keydown-роутер: event.code, typing-context,
                     модификаторы, e.repeat
    log.ts           кольцевой буфер → storage.local (ключ на контекст),
                     prime при старте, pagehide-флеш
    messaging.ts, errors.ts, escape.ts, selectors.ts, FloatingPanel.ts
  content/     — content-скрипт: index.ts (регистрация фич), фичи, панели
  background/  — SW (Chrome) / event page (Firefox): OBS-клиент, инжект автопринятия
  popup/       — настройки: дифф-запись, подписка на onChanged
  shared/types.ts — Settings + ExtMessage (вся матрица сообщений)
```

Ключевые механики:
- **Фичи** регистрируются в `content/index.ts`; `settingKey: null` = фича
  активна всегда (auto-start, tooltip, obs-panel), гейты — внутри неё.
- **Сообщения**: popup ↔ background ↔ content через `core/messaging`.
  Полифилл доставляет ответ ТОЛЬКО если хендлер вернул Promise.
- **Три бандла из одних исходников**: модульные let-переменные core/*
  существуют отдельно в каждом контексте. Это не общее состояние!

## 4. Инварианты (нарушишь — вернёшь старые баги)

1. **Каждая запись в DOM из подписчика `onDomChange` обязана быть
   идемпотентной** (сравнение перед записью) или задросселенной. Общий
   наблюдатель следит за class/style/childList всего документа: безусловная
   запись = самоподдерживающийся цикл на частоте кадров = «сайт сходит с ума,
   лечится перезапуском браузера». Так было в 6+ местах до 8.1.21.
2. **Автоклики**: только точное совпадение нормализованного текста (никаких
   подстрок — «Не готов» содержит «готов»!), только видимые элементы, бюджет
   попыток, бэкофф 5с после trusted-действий пользователя, гейт по контейнеру
   контекста (точная «Подтвердить» есть в любом диалоге сайта). textContent
   наследуется вниз — фильтр по тексту без deepest-only дедупа матчит
   родителя И дитя.
3. **Заметки живут в storage.local** — осознанное решение владельца (sync
   имеет лимит 8 КБ/ключ и молча терял заметки). НЕ возвращать в sync без
   нового решения. `obs_password` — только storage.local (`LOCAL_KEYS`).
4. **Попап пишет только дифф** против `lastKnown` и подписан на onChanged.
   Никогда не писать весь объект настроек снимком DOM.
5. **Хоткеи**: `event.code` (раскладко-независимо: KeyF = «а»!), обязательные
   гейты typing-context / модификаторы / repeat — и в роутере, И в любом
   отдельном listener'e (как dBlocker в role-faker).
6. **WebSocket-переподключение**: перед заменой сокета отвязать ВСЕ четыре
   хендлера старого (включая onopen!) и сделать полный teardown состояния
   (isConnected и пр.), иначе — залипшие статусы и вечный connecting.
7. **Симметрия жизненного цикла**: всё, что навесил enable(), снимает
   disable() — та же ссылка, тот же capture-флаг; хвостовые setTimeout —
   в именованных переменных и тоже гасятся.
8. **Формы данных матча** сверять с `legacy/match_314446.json`, а не с
   предположениями. Известное: `data.{players,votes,shots,checks}`; туры
   голосования `num: 0` = **выставление** (заявки кандидатур в речах) /
   `1` = **голосование** (основное) / `2` = **подъём всех** (ничья, «руки
   на столе»). Это термины сообщества спортивной мафии, согласованы с
   владельцем в 8.1.32-8.1.33 — НЕ «исправлять» на основной/переголосовку.
   **Подъём может увести НЕСКОЛЬКИХ игроков сразу** («попил»): при чётном
   столе тур 2 может повторить ничью тура 1 — и если стол проголосовал за
   подъём, уходят ВСЕ ничейные кандидаты. Реальный пример:
   `polemicagame.com/match/598995`, день 4 (6 живых, 3:3 в обоих турах →
   ушли №4 и №5). Любая структура «ушедший = одно число» такой день выразить
   не может — только список.
   ВНИМАНИЕ, это допущение: подъём — отдельный бюллетень и может НЕ пройти,
   тогда не уходит никто. Код (8.1.37) считает ушедшими всех ничейных, потому
   что отличить провалившийся подъём в данных пока нечем (§6 п.12).
   Роли: 0=дон, 1=мафия, 2=мирный, 3=шериф; `firstKilled` — это userId,
   НЕ позиция; guess бывает у нескольких игроков.
   Число дней НЕ фиксировано (может быть и 3, и 8+) — нигде не зашивать;
   `maxDay` считается из votes/shots/checks.
9. **Миграции storage** — атомарно (данные+флаг одним `set`), флаг
   per-device в local, sync-копию заметок не удалять (мост для вторых
   устройств). Разовые чистки — в `runUpgradeMigrations()`
   (background/index.ts) под собственным флагом.
10. При «оживлении» настройки, которую старый попап писал, но никто не
    читал, — обязательна разовая миграция: у всех активных пользователей в
    storage лежит бессмысленное старое значение (кейс
    twitch_floating_panel_enabled, 8.1.23→8.1.24).

## 5. Карта хранилища

| Область | Ключ | Кто |
|---|---|---|
| sync | все Settings кроме obs_password | settings.ts; точечные писатели: панели (закрытие крестиком) |
| sync | playerNotes, notes, tagCustomColors | ЗАМОРОЖЕНО: мост миграции для старых устройств, не трогать |
| local | playerNotes, tagCustomColors, pn_notes_migrated_v1 | notes-store.ts. Ключи заметок (8.1.29): `u:<userId>` (предпочтительно, вечный) + легаси-ники (фолбэк, лениво мигрируют) |
| local | obs_password | settings.ts (LOCAL_KEYS) |
| local | roleMarks | role-marker (метки «мой read», ключи g:<id> / l:<состав>) |
| local | polemica:logs:{content-<session>,bg,popup,ext} | log.ts (CAP 600/контекст; content-сессии старше 24ч чистятся) |
| local | obs_connection_state, obs_auto_scene_state | obs-client/obs-panel |
| local | obs_manual_disconnect, obs_retry_blocked | background (8.1.25): пауза «Отключиться» и блок реконнекта при 4008-4011; ОБА сбрасываются на onStartup |
| local | pn_twitch_panel_restored_v1 | флаг миграции 8.1.24 |
| localStorage страницы | fp:*, polemica:update*, polemica:loglevel/buflevel | панели/update-notify/log. ВНИМАНИЕ: пишется сайтом ⇒ недоверенный источник |

## 6. Техдолг (актуален на v8.1.28)

Отсортировано по (вред × вероятность). Файл:строка проверены на HEAD.
Перед работой перепроверь — код живой.

### Высокий приоритет
1. ✅ ЗАКРЫТО в 8.1.25 (opencode + доводка): chrome.alarms-watchdog,
   reconcile при пробуждении SW, persisted-блоки со сбросом на onStartup.
   Мёртвый patch history из isolated world удалён в 8.1.27; роутер использует
   polling 500мс + popstate.
2. ✅ ЗАКРЫТО в 8.1.25/8.1.28: единый URL-роутер (content/index.ts), route-sync
   у match-stats/player-notes, AbortController в match-data; pendingGameData
   после 10-секундного give-up сохраняется и задросселенно ретраится по DOM.
3. ✅ ЗАКРЫТО в 8.1.26: `loadPlayerStats` ищет ID вне активных игр через общий
   TTL-кэш `rating/get-list`, который переиспользуют статистика, профиль и
   история игр; отсутствие в топ-1000 честно показано в тултипе и по клику.
   Остаток: для игрока вне активной игры MMR не берётся из rating payload
   (форма поля не подтверждена), поэтому показывается `—`.
4. ✅ ЗАКРЫТО в 8.1.29: заметки ключуются `u:<userId>` с ЛЕНИВОЙ миграцией —
   ник-ключ переезжает на id, когда статистика резолвит игрока; чтение
   id-first с фолбэком на ник; «Vasya»/«vasya» сливаются (свежая побеждает),
   ник хранится в записи (`nick:`) для экспорта. Остаток: тёзки ДО первого
   резолва статистики неразличимы (честное ограничение); заметка, написанная
   до резолва, мигрирует на id ПЕРВОГО отрезолвленного тёзки.

### Средний приоритет
5. ✅ ЗАКРЫТО в 8.1.27: автомод OBS ищет текст только в контейнере стадий и
   понимает очевидные EN-маркеры (best-effort, не проверено на живом EN).
   Остаток: characterData не наблюдается общим observer'ом, поэтому in-place
   смена текста без childList/class/style может обнаружиться с задержкой.
6. ✅ ЗАКРЫТО в 8.1.26: drag/resize window-слушатели и pointercancel имеют
   симметричный cleanup, записи style коалесцируются через rAF, нулевой или
   отсоединённый бокс не сохраняется и не восстанавливается.
7. ✅ ЗАКРЫТО в 8.1.26: повторный enter гасит таймер, удаляет текущий tooltip
   и scoped-сироты этой точки до создания нового.
8. ✅ ЗАКРЫТО в 8.1.26: camera-flip хранит активные canvas/rAF, останавливается
   на отсоединённом video, откатывает неудачный flip и вызывает `unflipAll()`
   при disable фичи и выключении настройки поворота.
9. Частично закрыто в 8.1.28: content-вкладки пишут в отдельные session-ключи,
   неактивные дольше 24ч ключи чистятся. Остаток: `polemica:buflevel`
   управляется localStorage сайта — сайт может включить debug и собрать
   Twitch-чат в экспортируемый файл. → `log.ts:33-60`.
10. ✅ ЗАКРЫТО в 8.1.27: F8 кликает только видимую паузу/продолжение,
    исключает навигационные `<a href>` из кандидатов и закрывает меню,
    открытые неудачным перебором.
11. ✅ ЗАКРЫТО в 8.1.27: SVG ролей истории собирается через createElementNS и
    setAttribute; base/roleId больше не интерполируются в HTML-атрибуты.
12. Частично закрыто в 8.1.37: день ухода для точки «лучшего хода» считает
    `resolveDayOutcome` (общий с таймлайном) по исходу ФИНАЛЬНОГО тура, а не по
    выставлениям; `departed` — массив (ничья в подъёме уводит нескольких, см.
    §4 п.8), день без голосования и ничья в туре голосования ушедших не дают.
    Остаток: слабые условия ЛХ (fallback nights[0], сравнение со СТАРТОВЫМ
    числом мафии) → `findShotNight` + `addBestMoveIndicators`; колонки в порядке
    массива players без сортировки по position → `enhanceTable` (`players.forEach`);
    палитра ЛХ-тултипа отличается от ROLE_COLORS → `addDotToCell`.
    ОТКРЫТЫЙ ВОПРОС, от него зависит допущение в §4 п.8 (нужен сырой JSON
    матча с попилом): в 598995 день 4 тур 0 содержит шесть записей
    `candidate: 1` от ВСЕХ живых, при том что в турах 1-2 кандидатами были
    только №4 и №5. Кандидат, которого выставили шестеро из шести, не мог
    отсутствовать в бюллетене — значит это НЕ выставления, а похоже на
    бюллетень подъёма (1 = «за»). Если так: (а) мы сейчас рисуем шесть ложных
    «Выс» в ячейке №1 в видах hints/legend (в classic тур 0 не рисуется);
    (б) появится способ отличать провалившийся подъём — ожидаемый маркер
    «против» это `candidate: 0` или подобное.
    Как добыть сырые данные без сайта: временный
    `log.debug(SCOPE, "votes", JSON.stringify(gameDetails.votes))` в
    `enhanceTable`, владелец открывает матч с включёнными логами и выгружает
    их из попапа. Эвристики в рендере НЕ городить: проверенные варианты
    (выкинуть кандидата без голосов в турах ≥1 / повторную запись голосующего
    / самоголос) ломаются на законных данных 314446.

### Низкий приоритет / решения владельца
13. Фоновый инжект автопринятия дублирует контентный — убрать вместе с
    permission "scripting"? (Осознанно оставлен как страховка.)
14. Дефолты новичка агрессивны: auto_accept/skip_start/debug_logging = true
    из коробки. Продуктовое решение.
15. ✅ ЗАКРЫТО в 8.1.28: Twitch-статус в попапе приходит от реального
    WebSocket open/close, ожидание ограничено 5с, ошибки выводятся в
    twitch_status. Остаток: sendToTab глотает «нет получателя», но Twitch-путь
    честно завершается по таймауту. → `popup/index.ts`, `messaging.ts:22-28`.
16. `version` пишется в local при каждом сохранении заметок, никем не
    читается (карго-культ от legacy). → `notes-store.ts:163`.
17. Полифилл webextension-polyfill забандлен ×3 (~97 КБ суммарно) — в MV3
    Chrome почти не нужен.
18. isTypingContext слеп к shadow DOM; Enter не в producesText;
    contenteditable="plaintext-only" в Chromium. → `keyboard.ts:23-39`.
19. Гонки узких окон: миграция заметок из двух контекстов одновременно;
    RMW-импорт всей карты; role-marker last-write-wins в окне дебаунса 400мс.
20. nickname-lengths считает кодовые точки, а сайт, вероятно, UTF-16-юниты.
21. camera-flip: когда draw-цикл сам снимает переворот при ре-рендере плитки
    (!video.isConnected), кнопка поворота не переsync'ится — может остаться с
    opacity «1» при снятом перевороте (косметика). → `camera-flip.ts:~97`,
    `player-notes.ts:~884` (syncRotateButton).
22. camera-flip: activeFlips — Map с сильной ссылкой на video; в фоновой
    вкладке rAF заморожен, auto-cleanup не срабатывает до фокуса/unflipAll
    (транзиентно). → `camera-flip.ts:19`.
23. Заметки, ниты 2-го ревью 8.1.29 (все — только через крафтовые бэкапы):
    строка под u:-ключом перезатирается миграцией без слияния; смешанный
    бэкап (u:-запись + её ник-ключ) в пустую базу даёт дубль; разнотипные
    id ("0123" vs 123) дали бы разные ключи. → notes-store.ts, player-notes.ts.
24. pause-hotkey: «наши» открытые меню отслеживаются снапшотом ссылок
    (initialVisibleMenus, Set<Element>) — Vue-ре-рендер пересоздаёт узлы, и
    меню, открытое ПОЛЬЗОВАТЕЛЕМ до F8, может быть сочтено нашим и закрыто.
    → `pause-hotkey.ts` (ensureMenuOpen/closeCurrentAutomatedMenu).
25. F8 и автомод день/ночь после переписывания в 8.1.27 НЕ проверены в живой
    игре (точный словарь паузы, эвристика контейнера стадий, EN-маркеры) —
    первая же живая сессия должна включать: F8 постановка+снятие паузы,
    ночь→день→ночь при автомоде, F8 при открытом чужом диалоге.

## 7. Как аудитить этот проект

Проверенная методика (4 итерации, ~50 подтверждённых находок):

1. Запускай 5–7 узкоспециализированных субагентов ПАРАЛЛЕЛЬНО, каждому —
   свой срез (готовые промты: `docs/audit-prompts.md`).
2. Каждому агенту давай список УЖЕ ИЗВЕСТНЫХ находок («не повторяй») — иначе
   половина отчёта будет пересказом.
3. Требуй: файл:строка, конкретный сценарий пользователя, серьёзность, и
   ОБЯЗАТЕЛЬНО раздел «проверено и чисто» (отсутствие находок в области —
   тоже результат).
4. **Проверяй громкие находки сам, прежде чем чинить.** Агенты ошибаются:
   «рассинхрон формы payload» в match-stats был опровергнут реальным JSON
   (`legacy/match_314446.json`), а «XSS в баннере обновления» — чтением кода
   (там textContent). И наоборот: «аутентификация OBS не работала никогда»
   подтвердилась.
5. После каждой волны фиксов — отдельный агент на ревью СВЕЖЕГО диффа
   (регрессии находились в каждом релизе: acceptScanTimer, twitch onopen,
   завис connecting, регрессия twitch_floating_panel_enabled) и отдельный
   агент на «путь обновления» (что случится у живых пользователей со старым
   storage).
6. Сверка бэклога отдельным агентом: «проверь по коду, что из списка ещё
   открыто» — не верь списку на слово.

## 8. Краткая история (для контекста)

- **8.1.21** — «большой фикс стабильности»: 6+ MutationObserver-циклов
  (главная жалоба «сайт сходит с ума»), гонка FeatureManager, буфер мутаций.
- **8.1.22** — заметки sync→local (тихая потеря с ~45-й заметки), автопринятие
  на точное совпадение с бюджетами, хоткеи с typing-context, логи с prime.
- **8.1.23** — аутентификация OBS (не работала НИКОГДА, включая legacy),
  дифф-запись попапа, туры голосования num 0/1/2, выпил фич-призраков
  (аватар, :contains, мёртвые слушатели), чистка манифеста.
- **8.1.24** — автомод OBS отвязан от панели (не запускался без неё!), завис
  connecting, миграции обновления (возврат twitch-панели, чистка
  legacy-пароля из sync), инверсия tracked-роли, idle-watchdog Twitch.
- **8.1.25** — alarms-watchdog и восстановление OBS после выгрузки service
  worker; единый SPA URL-роутер и route-lifecycle статистики матча.
- **8.1.26** — статистика игроков вне активных игр, безопасный lifecycle
  FloatingPanel/tooltip/camera-flip.
- **8.1.27** — устойчивый F8, EN-маркеры и дешёвый детект стадий OBS,
  безопасный SVG истории и упрощённый SPA-роутер.
- **8.1.28** — честный Twitch-статус, раздельные логи content-вкладок,
  поздний retry статистики матча и безопасное восстановление скрытых камер.

Полные отчёты аудитов — в истории коммитов и release notes на GitHub.
