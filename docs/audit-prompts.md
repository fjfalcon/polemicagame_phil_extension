# Библиотека аудит-промтов

Боевые промты четырёх аудитов Polemica Notes (июль 2026), давшие ~50
подтверждённых находок. Адаптированы для переиспользования любым ИИ-агентом.

## Как пользоваться

1. Запускай промты ПАРАЛЛЕЛЬНЫМИ субагентами — они спроектированы
   непересекающимися.
2. В каждый промт подставь блок `УЖЕ ИЗВЕСТНО:` — актуальный список открытых
   пунктов из AGENTS.md §6 плюс всё, что нашли предыдущие волны. Без этого
   агент потратит половину отчёта на пересказ известного.
3. Обязательные требования к каждому агенту (уже вшиты в промты, не удаляй):
   - файл:строка для каждой находки;
   - конкретный сценарий «пользователь делает X → происходит Y»;
   - серьёзность (блокер/высокая/средняя/низкая);
   - явный раздел «проверено и чисто»;
   - НЕ предлагать патчи (аудит отделён от фиксов);
   - читать файлы целиком, а не грепать.
4. Громкие находки проверяй сам до фиксов: реальный JSON матча лежит в
   `legacy/match_314446.json`, старое поведение — в `legacy/*.js`,
   происхождение кода — `git log -S`.
5. После волны фиксов гоняй промты №7 (дифф) и №8 (путь обновления) — в
   каждом релизе этого проекта находились регрессии свежего кода.
6. Раз в ~5 релизов — промт №11 (мета-ревью самих промтов); перед волной по
   незнакомому классу изменений — канарейка №12.
7. №7 ОДИНОЧНЫЙ допустим только для точечного диффа. Волна с новой фичей
   обязана включать №10 (минимум сценарий «Обещание из попапа»), а любое
   касание настроек — №8 п.6: обе прод-жалобы 9.28.x лежали в НЕзапущенных
   №8/№10 (мета-ревью №11, 26.08.2026).
8. Вердикт «код корректен» для фичи с пользовательским обещанием не
   принимается без трассы маршрута обещания (файл:строка каждого гейта).

---

## №1. Жизненный цикл и утечки

> Ты аудируешь браузерное расширение в <PATH> (src/, TypeScript). Игнорируй legacy/.
>
> ТВОЯ ТЕМА: симметрия жизненного цикла и утечки ресурсов.
>
> Архитектура: src/core/feature.ts — FeatureManager, у каждой фичи
> enable(ctx)/update(ctx)/disable(). Панели: src/core/FloatingPanel.ts.
>
> Проверь СТРОГО по каждому файлу в src/content/**, src/core/**, src/background/**:
> 1. Каждый setInterval/setTimeout из enable() — очищается ли в disable()? Есть ли
>    переменная-хендл, обнуляется ли?
> 2. Каждый addEventListener — снимается ли той же ссылкой и с тем же capture-флагом?
> 3. Каждый onDomChange(...) — сохраняется ли off-функция и вызывается ли?
> 4. Каждый Mutation/Resize/IntersectionObserver — disconnect()?
> 5. Модульные let-переменные: сбрасываются ли в disable()? Что при двойном enable()?
> 6. Панели: unmount() снимает ли всё, что навесил mount()/renderBody()?
> 7. DOM-узлы в document.body, которые не удаляются.
>
> УЖЕ ИЗВЕСТНО: <вставь актуальный список>
>
> Для каждой находки: файл:строка, что течёт, сценарий пользователя, серьёзность.
> НЕ предлагай патчи. Если область чиста — перечисли, что именно проверил.

## №2. Хранилище и сохранность данных

> ТВОЯ ТЕМА: сохранность пользовательских данных. Заметки об игроках — самое
> ценное в расширении, их потеря недопустима.
>
> Проверь:
> 1. Гонки read-modify-write: чтение объекта → модификация → запись целиком.
>    Две вкладки одновременно? Что затирается?
> 2. Квоты: storage.sync = 8КБ/элемент, 100КБ всего, 120 записей/мин.
>    Обрабатываются ли ошибки? Врёт ли UI при неудачной записи?
> 3. storage.local: неограниченный рост? Чистится ли когда-нибудь?
> 4. Ключи: коллизии префиксов (спецсимволы/`__proto__` — в №5, здесь не дублировать).
> 5. Экспорт/импорт: валидация, старые форматы, битые файлы, честность тостов.
> 6. Вызовы storage без try/catch — что упадёт в приватном режиме/при квоте.
> 7. Миграции форматов между версиями: атомарность (данные+флаг одним set),
>    per-device флаги, повтор после сбоя.
> 8. Попап и опции — полноправная область проверки: контрол, отображающий
>    состояние по иному правилу, чем судит потребитель этого состояния, —
>    находка (мета-ревью №11: src/popup не входил в область ни одного промта).
>
> УЖЕ ИЗВЕСТНО: <...> Формат ответа — как в №1.

## №3. Асинхронность, сообщения, фон

> ТВОЯ ТЕМА: async-корректность и обмен сообщениями popup ↔ background ↔ content.
>
> Контекст: Chrome MV3 = service worker (умирает через ~30с простоя, теряет
> память и сокеты), Firefox MV3 = event page. webextension-polyfill доставляет
> ответ ТОЛЬКО если хендлер вернул Promise.
>
> Проверь:
> 1. Состояние в памяти фона: что теряется при выгрузке SW? Есть ли
>    chrome.alarms/keepalive? Восстановление при пробуждении?
> 2. Хендлеры сообщений: Promise-ответы, ветки без ответа, «Receiving end
>    does not exist».
> 3. Гонки несериализованных async-функций; подписки, оформленные ПОСЛЕ
>    первого асинхронного прохода (окно потери событий).
> 4. WebSocket: реконнект (backoff? лимит? сброс лимита?), замена сокета
>    (ВСЕ 4 хендлера старого отвязаны, включая onopen? полный teardown
>    состояния?), reject промиса коннекта при закрытии до готовности,
>    таймауты pending-запросов, JSON.parse в onmessage.
> 5. Promise «в никуда»: void/then без catch — где ошибка реальна.
> 6. События CustomEvent без буферизации: подписчик мог опоздать?
>
> УЖЕ ИЗВЕСТНО: <...> Помечай, специфична ли находка для Chrome или Firefox.

## №4. Горячие пути DOM и циклы обратной связи

> ТВОЯ ТЕМА: производительность и циклы «расширение будит само себя».
>
> Контекст: единый MutationObserver на документ
> ({childList,subtree,attributes,attributeFilter:[class,style]}, rAF-батчинг).
> ЛЮБАЯ запись подписчика в class/style/childList будит ВСЕХ подписчиков.
>
> Проверь:
> 1. ВСЕ вызовы onDomChange: что пишет каждый подписчик? Идемпотентно
>    (сравнение перед записью)? Задросселено? Ищи ВТОРОЙ путь записи в уже
>    «починенных» местах (ранний выход может стоять после дорогого чтения!).
> 2. Косвенные циклы: A пишет то, на что реагирует B, и наоборот. Построй карту.
> 3. Стоимость прохода: сколько querySelectorAll на батч суммарно; селекторы
>    [class*=...]; textContent всего body; O(n²)-дедупы.
> 4. Все setInterval: период, стоимость тика, ранний выход.
> 5. Высокочастотные события (pointermove при drag панелей!) → записи style →
>    пробуждение наблюдателя покадрово.
> 6. Layout thrashing: getComputedStyle/getBoundingClientRect вперемешку с
>    записями стилей в циклах по игрокам.
>
> УЖЕ ИЗВЕСТНО: <...> Отдельно перечисли подписчиков/интервалы, признанные
> безопасными, с обоснованием.

## №5. Безопасность и приватность

> ТВОЯ ТЕМА: безопасность, приватность, требования магазинов (CWS/AMO).
>
> Проверь:
> 1. Все innerHTML/outerHTML/insertAdjacentHTML: каждая интерполяция — через
>    escapeHtml? Отдельно: URL-контексты (href="...") и CSS-контексты
>    (cssText, background:) — escapeHtml их НЕ покрывает.
> 2. Секреты (пароль OBS): все места чтения/записи/передачи/логирования.
>    Не рассылается ли по вкладкам? Не остались ли КОПИИ В SYNC от старых
>    версий (storage.sync.remove в коде вообще есть?)?
> 3. Логи и экспортируемый файл: URL страниц (пригласительные токены!), UA,
>    чужие ники/чаты. Кто управляет уровнем логирования (localStorage
>    страницы = недоверенный)?
> 4. Манифест: каждое permission реально используется? matches https-only?
>    WAR — одной строкой подтверди прежний состав. Прогони web-ext lint по
>    КАЖДОМУ таргету отдельно, БЕЗ пайпов, и приведи exit-код каждого прогона;
>    затем найди, кто читает этот exit-код в бою (package.json, релиз-гейт).
>    Проверка, чей результат глотается (| tail, | grep) или падает всегда и
>    все привыкли, — находка (класс lint:ext:chrome, 9.34.0).
> 5. CSP: подтверди одним предложением, что внешних скриптов/eval нет
>    (полный свип сокращён мета-ревью №11 — класс структурно невозможен
>    при TS-сборке без внешнего кода).
> 6. Сетевая карта: полный список исходящих запросов, схемы, переменные хосты.
> 7. onMessage: подтверди, что externally_connectable по-прежнему отсутствует;
>    рантайм-валидация payload только для НОВЫХ обработчиков.
>    innerHTML-свип: только sinks, добавленные после последнего полного
>    (полный — раз в ~10 релизов).
> 8. Что расширение кладёт в DOM страницы (сайт ЭТО читает): заметки, реды,
>    сцены OBS — оцени приватность.
>
> УЖЕ ИЗВЕСТНО: <...>

## №6. Устойчивость и UX-неожиданности

> ТВОЯ ТЕМА: поведение, которое удивляет или вредит пользователю.
>
> Проверь:
> 1. ВСЕ места, где код кликает за пользователя: условия срабатывания, точное
>    совпадение vs подстрока (textContent наследуется!), видимость, бюджеты,
>    бэкофф после действий пользователя, гейт по странице/контейнеру.
>    Может ли сработать во время голосования/в чужом диалоге?
> 2. Хоткеи: typing-context (включая ОТДЕЛЬНЫЕ listener'ы мимо роутера),
>    русская раскладка (event.code!), модификаторы, repeat, конфликты кодов.
> 3. Ломкость селекторов: null-обработка, вечные ретраи вместо тихой сдачи,
>    коллизии «наших» и сайтовых классов, точное равенство русских текстов
>    («идет» vs «идёт»), захардкоженные Vue-хэши [data-v-...].
> 4. Двуязычность: RU-тексты фаз/кнопок — что на EN-интерфейсе?
> 5. Числа: деление на ноль (Infinity%), NaN, valid-ноль vs «нет данных».
> 6. Обратимость: выключил фичу — всё ли вернулось (display, position,
>    innerHTML заголовков, перевёрнутые камеры)?
>
> УЖЕ ИЗВЕСТНО: <...>

## №7. Ревью свежего диффа (запускать после КАЖДОЙ волны фиксов)

> Ты — злой ревьюер. ТВОЯ ТЕМА: коммит <SHA> — найди РЕГРЕССИИ именно в новом
> коде. Сначала git show <SHA>, потом каждый затронутый файл ЦЕЛИКОМ —
> интеграция нового со старым важнее самого диффа.
>
> Особо проверяй: (а) половинчатые фиксы — починили путь A, а симметричный
> путь B остался (пример: отвязали onclose/onerror/onmessage, забыли onopen);
> (б) фиксы, меняющие семантику данных, — обновлены ли ВСЕ читатели (пример:
> перевели туры голосования на num=0, а findVotedDay остался на num=1);
> (в) новые гейты/ранние выходы — не отрезали ли они легитимный путь;
> (г) смена дефолта настройки — что у СУЩЕСТВУЮЩИХ пользователей в storage
> (старый код мог писать ключ явно!); (д) оптимистичные обновления состояния
> до подтверждения записи; (е) эхо storage.onChanged в контексте-писателе;
> (ж) таймеры/промисы, переживающие disable;
> (з) для каждой новой фичи или расширения её области: возьми ОБЕЩАНИЕ из
> названия/релиз-строки («чат ВЕЗДЕ», «на всём сайте», «и вне игры») и
> проиграй маршрут из состояния, максимально далёкого от игровой комнаты
> (поиск, лобби, профиль, чистая загрузка) — от enable() до видимого
> пользователем результата, цитируя файл:строку КАЖДОГО гейта по пути,
> ВКЛЮЧАЯ старые гейты, написанные до фичи. Гейт, требующий контекста,
> которого обещание не требует, — блокер (класс «чат везде», 9.28.0).
>
> УЖЕ ИЗВЕСТНО: <...> Блокер/высокая/средняя/низкая + «проверено, чисто».

## №8. Путь обновления живого пользователя

> ТВОЯ ТЕМА: у реальных пользователей в storage лежит состояние, записанное
> ВСЕМИ прошлыми версиями (включая legacy до рефакторинга). Расширение
> обновляется автоматически без чистки.
>
> 1. Составь полную карту ключей storage: кто пишет, кто читает, сироты
>    старых версий (git show <old>:<file> и legacy/*.js — источники правды).
> 2. Прогони сценарии по коду шаг за шагом: обновление через одну версию;
>    очень старая версия (другие ключи/форматы); два устройства с sync;
>    downgrade и возврат (флаги миграций!).
> 3. Смены дефолтов: старый попап писал ключ явно? Тогда дефолт не сработает
>    ни у одного реального пользователя.
> 4. Пермишены: сужение безопасно, расширение = disable до подтверждения.
>    Сужение matches — проверь редиректы живым curl.
> 5. Форматы persisted-состояния (OBS state и пр.) — переживёт ли читатель
>    старый/битый формат?
> 6. Для КАЖДОГО ключа настроек выпиши ПРЕДИКАТ каждого читателя: попап (что
>    рисует), FeatureManager (что решает: === true), миграции, background.
>    Расхождение предикатов — находка, даже если сегодня в storage «правильный»
>    тип. Прогони исторические значения (true, 1, "true", undefined — по
>    git show старых версий и legacy/) через каждый предикат: где UI скажет
>    «включено», а фича не запустится? (класс «кнопок нет», 9.28.1)
>
> УЖЕ ИЗВЕСТНО: <...> Серьёзность: потеря данных / сломанное поведение / мусор.

## №9. Сверка бэклога

> ТВОЯ ТЕМА: не верь бэклогу на слово. Для КАЖДОГО пункта списка ниже проверь
> по ТЕКУЩЕМУ коду: открыт / починен / частично. Формат: таблица
> «пункт | статус | файл:строка | серьёзность | комментарий 1-2 предложения».
> Пройдись по git diff <last-audit-sha>..HEAD --stat и назови пункты,
> затронутые частично. В конце — ТОП-10 открытых по (вред × вероятность).
>
> СПИСОК: <вставь AGENTS.md §6 целиком>

## №10. «Свежие глаза»: сквозные сценарии

> ТВОЯ ТЕМА: не ищи по файлам — прогони СКВОЗНЫЕ пользовательские сценарии по
> коду, от события к событию, через границы модулей, цитируя файл:строку
> каждого перехода. На каждом шаге: что реально произойдёт? в каком порядке?
> что увидит пользователь? какие статусы UI врут?
>
> Сценарии (адаптируй под текущие фичи):
> - «Вечер стримера»: старт браузера → игра → 3 часа → несколько смертей SW.
> - «Заметочник»: 40+ заметок, смена игроков на слотах, F5.
> - «Две вкладки»: параллельные правки меток/заметок, закрытие одной.
> - «Новичок с дефолтами»: что происходит БЕЗ его ведома (автоклики, запросы)?
> - «Хоткей в бою»: двойные нажатия, гонки с автоматикой, isTrusted-фильтры.
> - «Смена ника in-place»: characterData-слепота наблюдателя.
> - «Экспорт логов для багрепорта»: что РЕАЛЬНО в файле, чего не хватает,
>   что пользователь не хотел бы отправлять.
> - «Обещание из попапа»: возьми формулировку каждого тумблера попапа и
>   релиз-нот ДОСЛОВНО и проиграй её на самой непохожей странице сайта;
>   каждый молчаливый return по пути — находка, даже если код «корректен
>   по чтению» (мета-ревью №11, 26.08.2026).
>
> Топ-5 «сюрпризов для пользователя» в конце.

## №11. Мета-ревью промтов (раз в ~5 релизов; контур учит сам себя)

> Ты аудируешь не код, а ПРОЦЕСС РЕВЬЮ расширения в <PATH>.
>
> Входные данные:
> 1. Промты №1–№10 этого файла (прочитай целиком).
> 2. Отчёты последних adversarial-волн: <вставь или укажи коммиты fix-волн —
>    их сообщения перечисляют находки>.
> 3. Жалобы пользователей, дошедшие до прода ПОСЛЕ ревью: <вставь список;
>    источник — история AGENTS.md §8 и коммиты «жалоба N.N.2026»>.
>
> ВОПРОС ОДИН: какие КЛАССЫ багов пропускает текущий набор промтов?
> Для каждого пропущенного прод-бага ответь: (а) какой промт ДОЛЖЕН был его
> поймать; (б) почему не поймал — слепое пятно формулировки, отсутствие
> сценария, или ревьюер не прочитал нужный файл; (в) какую строку добавить
> в какой промт, чтобы класс закрылся. Предлагай ПРАВКИ ПРОМТОВ, не кода.
> Отдельно: какие пункты промтов НИ РАЗУ ничего не находили — кандидаты на
> сокращение (раздутый промт читается хуже короткого).
>
> Известные закрытые пропуски (для калибровки, не повторять): «чат везде»
> 9.28.0 — код корректен по чтению, но пользовательский маршрут не проигран;
> вечный DOM-цикл карточек 9.32.1 — тест идемпотентности не доигрывал
> мутацию от самоудаления (теперь исполняемый страж: dom-fixpoint).

## №12. Канарейка: проверка самого ревьюера (выборочно, перед большой волной)

> Техника, не промт. Перед тем как доверять волне ревью НЕЗНАКОМОГО класса
> изменений:
> 1. Подсади в рабочую копию ОДНУ синтетическую регрессию известного класса
>    (не коммить!): убери гейт идемпотентности, поменяй `===` на `!==` в
>    сравнении настройки, сними флаг ПЕРЕД подтверждением операции, убери
>    `slice` у потолка буфера.
> 2. Запусти обычный промт №7 на дифф с подсадкой.
> 3. Ревьюер обязан найти подсадку. Не нашёл — волне не доверять: чинить
>    промт (№11), а не полагаться на «повезёт».
> 4. Подсадку откатить ДО настоящей волны (git checkout), в отчёте волны
>    зафиксировать: «канарейка N класса — поймана/пропущена».
>
> 5. Та же техника — для АВТОМАТИЧЕСКИХ гейтов, не только ревьюера: раз в
>    ~5 релизов сломай нарочно по одному входу каждой проверки контура
>    (typecheck, тесты, lint, release:assets) и убедись, что пайплайн
>    краснеет. Проверка, не умеющая падать, хуже отсутствующей — она
>    производит ложную уверенность.
>
> Прецедент 26.08.2026: мутационная проверка dom-fixpoint-стража (откат
> hiddenFor → 3 красных теста) — та же логика на уровне тестов.

## Специализированные (по мере надобности)

- **Протокол obs-websocket v5**: Hello(op:0) c {salt,challenge} →
  authentication = base64(SHA256(base64(SHA256(password+salt)) + challenge));
  Identified op:2; события Scenes; коды 4008/4009/4010/4011. Сверять с кодом
  identify()/handleMessage().
- **Протокол Twitch IRC**: PING/PONG, CAP REQ (без него нет тегов и
  commands), склейка \r\n во фрейме, JOIN lowercase, нормализация имени
  канала, детект молчаливо мёртвого сокета (~5 мин тишины), rate limits.
- **Корректность match-stats**: ВСЁ сверять с legacy/match_314446.json
  (формы, num-семантика туров, роли, firstKilled=userId).

## Арх-аудит швов: контракты и правила после декомпозиции (проверен 29.08.2026: 9 подтверждённых находок, из них 2 HIGH — лучший счёт среди спецпромтов)

Когда запускать: после крупной декомпозиции или смены схемы записи (интенты,
новые модули). Первый повод — разрез player-notes на 13 модулей и перевод
заметок/палитры на интент-записи (28.08.2026).

```
Ты — внешний аудитор архитектуры браузерного расширения Polemica Notes
(TypeScript, MV3, Chrome + Firefox). Тебе дан полный доступ к репозиторию.
Твоя задача — НЕ предлагать новую архитектуру, а найти дефекты в швах
существующей. Проект недавно прошёл крупную декомпозицию (монолит
player-notes.ts разрезан на 13 модулей по владению состоянием) и перевод
записей на интенты через фоновый координатор. Именно после таких волн швы
оказываются не там, а правила, записанные в двух местах, начинают
расползаться.

СНАЧАЛА прочитай целиком: AGENTS.md (особенно §3 «Архитектура», §4
«Инварианты», §5 «Карта хранилища»), затем src/content/features/player-notes.ts
и все модули src/content/features/player-notes/*, src/background/
notes-coordinator.ts, src/core/notes-store.ts, tests/invariants/
architecture.test.ts. Файлы читай целиком, не грепай выборочно.

ПЯТЬ НАПРАВЛЕНИЙ (по каждому — отдельный раздел отчёта):

1. ПРАВИЛА, ЗАПИСАННЫЕ ДВАЖДЫ. Координатор и вкладочный фолбэк нарочно
   дублируют правила записи (безопасные ключи, лимиты, правила миграции) —
   это задокументированное решение. Твой вопрос не «почему дубль», а «что
   удерживает дубли от расползания»: для КАЖДОЙ пары правил найди оба места
   (файл:строка + файл:строка) и тест, который сломается, если их развести.
   Пары без такого теста — находка. Ищи и другие дубли: попап (импорт/
   экспорт/лимиты), персист-списки (мьют/скрытие), палитра, роль-метки.

2. НАПРАВЛЕНИЕ ЗАВИСИМОСТЕЙ. Слои: shared → core → content|background|popup.
   Фичи не должны знать друг о друге; core не должен знать о фичах и фоне.
   Построй фактический граф импортов и назови каждое ребро против слоя.
   Отдельно: не «знает» ли модуль о соседе через строковые ключи хранилища,
   имена CSS-классов или дата-атрибуты, минуя реестры (selectors.ts, ключи
   в §5)?

3. КОНТРАКТЫ ВКЛАДКА ↔ ФОН. Перечисли все типы сообщений (runtime.sendMessage
   / порты) с файл:строка отправителя и получателя. Для каждого: что
   происходит при незнакомом сообщении, при ответе-мусоре, при закрытии
   канала на середине? Отдельный сценарий: расширение обновилось, вкладка
   ещё держит СТАРЫЙ content-скрипт, фон уже НОВЫЙ (или наоборот, Firefox
   не перезапускает вкладки) — какие сообщения станут взаимно непонятны и
   что увидит пользователь?

4. ВЛАДЕНИЕ СОСТОЯНИЕМ ПОСЛЕ РАЗРЕЗА. Для каждого модуля player-notes/*
   назови: каким состоянием он владеет, кто ещё его читает/пишет и через
   что (метод, колбэк контекста, прямое поле). Находки: два владельца у
   одного состояния; модуль, изменяющий чужое состояние мимо владельца;
   контекст-интерфейс, который врёт о своей роли; остатки монолита в
   player-notes.ts, которые по кластеру владения принадлежат уже
   вынесенному модулю.

5. ОБЕЩАНИЯ БЕЗ СТРАЖЕЙ И СТРАЖИ БЕЗ ОБЕЩАНИЙ. Сверь AGENTS §4 с
   tests/invariants/: для каждого инварианта — какой тест его закрепляет и
   упадёт ли тест, если инвариант нарушить самым ЛЕНИВЫМ способом (не
   экзотикой). Инварианты без стража — находка; стражи, не выводимые ни из
   одного обещания §4, — тоже (мёртвый груз или недокументированное
   обещание). Особо: §4.7 (симметрия enable/disable) проверяется только для
   features/** — панели (content/panels/*), попап и core не покрыты; пройди
   их симметрию ВРУЧНУЮ и перечисли несимметричные пары с файл:строка.

УЖЕ ИЗВЕСТНО (не тратить отчёт на пересказ):
- дубль правил координатор/фолбэк — осознанное решение, вопрос только в
  тестах, удерживающих пары (направление 1);
- границы AST-стражей перечислены в AGENTS §6 («Известные границы стражей»):
  шаблонные строки в лог, Object.assign для obs_host, className вместо
  classList, §4.7 без сопоставления цели — известны, повторять не надо;
- редизайн сайта трогает ТРИ места (реестр, notes.css, CSS-блоки семи
  фич) — задокументировано в selectors.ts и AGENTS §4;
- заметки в storage.local, sync — замороженный мост миграции (решение
  владельца); WS-фичи отложены; camera-health оставлен как есть (решение
  владельца 27.08.2026); browser-E2E с установленным расширением не делаем
  (AGENTS §2а);
- player-notes.ts ~2300 строк под инвариантом-капом, сегрегация идёт
  волнами — «файл всё ещё большой» без конкретного кластера владения не
  находка.

ФОРМАТ. Каждая находка: файл:строка; конкретный сценарий «пользователь/код
делает X → происходит Y»; серьёзность (блокер / высокая / средняя / низкая);
чем доказана (трасса кода, а не «кажется»). Обязателен раздел «Проверено и
чисто» — что смотрел и не нашёл, с той же конкретикой. Патчи НЕ предлагать:
аудит отделён от фиксов. Переписывания «на шину сообщений / стейт-менеджер /
фреймворк» не предлагать — только дефекты текущих швов. Если по направлению
чисто — так и напиши, пустое направление это тоже результат.
```

Примечание: блок «УЖЕ ИЗВЕСТНО» перед запуском сверь с AGENTS §6 — список
живёт.

---

## Аудит скрытия ролей на стриме: все механизмы и их наложения (заведён 29.08.2026, ещё не обкатан)

Когда запускать: после любой правки в цепочке «видимость роли» (auto-start,
obs-panel, role-faker) и перед стримовыми релизами. Это единственная зона,
где цена ошибки — не «неудобно», а «роль игрока уехала в эфир».

```
Ты — внешний аудитор браузерного расширения Polemica Notes (TypeScript, MV3,
Chrome + Firefox). Полный доступ к репозиторию. Предмет аудита ОДИН: скрытие
роли игрока на стриме. Недавно здесь чинили баг «подсмотреть роли (V)
показывает все роли, КРОМЕ своей» — он случился потому, что механизмов
скрытия несколько и снимаются они по отдельности. Твоя задача — проверить
ВСЕ механизмы, влияющие на видимость роли, и КАЖДОЕ их попарное наложение.

Иерархия вреда (ей подчиняется серьёзность):
- роль ВИДНА в эфире, когда должна быть скрыта, — блокер;
- роль НАВСЕГДА скрыта от самого игрока (не вернуть без F5) — высокая;
- роль мигнула/вернулась с задержкой — средняя/низкая по длительности.
Для КАЖДОГО найденного пути отказа явно скажи, в какую из сторон он падает.

СНАЧАЛА прочитай целиком: AGENTS.md §4, src/content/features/auto-start.ts,
src/content/panels/obs-panel.ts, src/content/features/role-faker.ts,
src/core/keyboard.ts, tests/invariants/obs-role-latch.test.ts и все юнит-тесты
этих файлов (tests/unit/). Файлы читай целиком.

ШАГ 1 — КАРТА ПИСАТЕЛЕЙ. Перечисли каждую точку, которая пишет в видимость
роли, с файл:строка и МЕХАНИЗМОМ записи. Известные (проверь и дополни):
  • auto-start: CSS-инъекция <style id=polemica-role-hide> (stylesheet
    !important, прячет роли ВСЕХ игроков);
  • auto-start: «подсмотреть, пока держу» (V) — снимает/возвращает ТРИ слоя
    (наш CSS, нативное скрытие своей роли самим сайтом через #stop,
    inline-путь setRoleVisibility);
  • auto-start: клавиша скрытия (D) + досылка нативного тумблера сайта;
  • obs-panel (автосмена сцен): applyRoleVisibility — inline-пин
    visibility/opacity с !important на каждом элементе, retry-машина
    scheduleRoleVisibility, латч lastAppliedRoleVisibility,
    restoreRoleVisibility на teardown;
  • role-faker (F/E): hideOtherRoles / resetRoles — inline display/visibility
    с картой originalRoles «как было»;
  • сам САЙТ: нативное скрытие своей роли и перерисовки Vue, стирающие наши
    inline-правки при пересоздании элементов.
Построй таблицу СИЛЫ: кто кого перебивает (inline !important > stylesheet
!important > обычные стили сайта) и кто выживает при пересоздании элемента.

ШАГ 2 — ВОЙНЫ СНИМКОВ. Каждый механизм хранит «как было» и восстанавливает.
Проверь каждую пару на снимок ЧУЖОГО состояния: механизм A снял снимок, пока
действовал пин механизма B → B восстановился → A восстанавливает свой снимок
и ВОЗВРАЩАЕТ пин B (или наоборот, стирает его). Конкретно проверь минимум:
originalRoles фейкера vs inline-пин obs-panel; три слоя peek vs пин
obs-panel; restoreRoleVisibility vs слои peek, снятые в этот момент;
пересоздание элемента сайтом между снимком и восстановлением (снимок держит
мёртвый узел — что восстановится?).

ШАГ 3 — МАШИНЫ И ЖИЗНЕННЫЙ ЦИКЛ. Для каждого механизма — что происходит при:
выключении его настройки В МОМЕНТ действия (peek удерживается, ретрай в
полёте, фаза подтверждается); disable() всей фичи; SPA-переходе из комнаты и
обратно; F5; автообновлении расширения (осиротевший content-скрипт — какие
пины/стили ПЕРЕЖИВУТ его смерть в DOM страницы и кто их снимет?);
obs_disconnected; смене фазы день↔ночь посреди активного peek/фейка;
двух вкладках с одной комнатой.

ШАГ 4 — КЛАВИШИ. V/D/F/E: попарные наложения (V во время D-скрытия, F во
время peek, E во время дня автомода...), удержание с автоповтором, отпускание
вне окна (pointerup/keyup ушёл в другой элемент/окно), isTypingContext,
переназначение клавиш в попапе на одну и ту же (захват уже чинили — SEAM-07),
клавиша, совпавшая с нативной горячей клавишей сайта.

УЖЕ ИЗВЕСТНО (не пересказывать):
- трёхслойный фикс peek 29.08.2026 (7bdb29f): startPeek снимает все три
  слоя, stopPeek возвращает каждый, нативное скрытие возвращается всегда с
  ретраями, таймер возврата гасится в disable();
- adversarial 29.08.2026 по obs-panel: pendingRoleVisibilityTimer гасится в
  stopDOMMonitoring, в колбэке гейт autoModeEnabled, включение автомода
  восстанавливает видимость текущей фазы, 30-мс await перепроверяется;
- OP-1: латч «скрыто» не ставится в обход применения, терминальная строка в
  логе; страж — tests/invariants/obs-role-latch.test.ts (source-based);
- inline-пин сильнее stylesheet-!important — задокументировано в коде.
Эти фиксы можно и НУЖНО пытаться опровергнуть глубже (обход, соседний путь),
но не пересказывать как находки.

ФОРМАТ. Каждая находка: файл:строка; сценарий «стример делает X → в эфире Y»;
направление отказа (в эфир / от игрока); серьёзность; чем доказано (трасса,
не «кажется»). Обязателен раздел «проверено и чисто» — с той же конкретикой,
по каждому шагу. Патчи НЕ предлагать. Переписывание механизмов на «единый
менеджер видимости» НЕ предлагать — только дефекты текущих наложений. Если
пара механизмов чиста — назови её и скажи почему.
```

Примечание: блок «УЖЕ ИЗВЕСТНО» перед запуском сверь с последними волнами —
список живёт.

---

## Аудит наблюдаемости: можно ли разобрать жалобу по логу (заведён 02.08.2026)

Повод: за один день две жалобы — «автовозврат в поиск не работает» и
«перестала работать автосмена сцен в OBS». В обоих случаях расследование
упёрлось в одно и то же: ВСЕ решения этих подсистем логировались на `debug`,
а в файл, который пользователь отправляет в поддержку, попадает только `info`.
Про автовозврат в логе не было ни строки за 80 секунд в комнате; про автосцены
— ни строки вообще. Первую жалобу удалось закрыть только чтением бандла сайта,
вторую — не удалось.

Цель аудита: сделать так, чтобы ЛЮБАЯ типовая жалоба закрывалась одним логом,
и при этом лог не превратился в шум. Это аудит НАБЛЮДАЕМОСТИ, а не поиска
багов: находка здесь — «этот отказ невидим», а не «этот код неправильный».

Промт (отдавать свежему агенту как есть):

> Ты — аудитор наблюдаемости браузерного расширения Polemica Notes
> (Chrome/Firefox MV3, TypeScript). ЗАДАЧА: определить, какие сбои расширения
> НЕВОЗМОЖНО диагностировать по логу, который пользователь выгружает из
> попапа. Итог — ОТЧЁТ, код НЕ менять: находки пойдут на adversarial review.
> Перед началом прочитай AGENTS.md §3, §4 и `src/core/log.ts` (уровни, буфер,
> что реально попадает в файл: `bufferLevel`).
>
> Метод. Для каждой пользовательской фичи составь список СОСТОЯНИЙ ОТКАЗА —
> ситуаций, в которых фича молча ничего не делает. Для каждого ответь:
> (а) попадёт ли в файл лога хоть одна строка; (б) хватит ли её, чтобы
> отличить это состояние от соседних; (в) если нет — какая строка нужна и на
> каком уровне. Отправная точка — фичи с гейтами и латчами: queue-requeue,
> auto-start (автопринятие, стартовый экран, камера), obs-panel (автосмена
> сцен, видимость ролей), obs-client (соединение, повторы, блокировки),
> queue-guard, queue-peek, player-notes (запись заметок), twitch-panel,
> role-faker, role-marker — И САМА ИНФРАСТРУКТУРА: FeatureManager (пропуск
> фичи по настройке обязан оставлять строку — урок 9.28.1), роутер сообщений,
> миграции. Перечисление фич не должно скоупить слепоту (мета-ревью №11).
>
> Направления атаки (по каждому — вердикт «чисто» или находка):
> 1. **Молчаливые ранние выходы.** Найди по AST все `return` в начале функций,
>    охраняемые условием, без записи в лог. Отдели безобидные (нечего делать)
>    от значимых (пользователь ждал действия, а его не будет).
> 2. **Латчи.** Переменные вида `xxxDone`/`gameStarted`/`armed`, которые
>    выключают подсистему до конца жизни страницы. Каждый обязан объявлять о
>    себе в момент взведения: именно такой латч сломал автовозврат.
> 3. **Персистентные блокировки.** Флаги в storage, переживающие перезапуск
>    (`obs_retry_blocked`, счётчик попыток, владелец сцены, мосты в
>    sessionStorage). Видно ли в логе, что состояние взведено и почему; есть
>    ли путь, где оно не снимается без ручного вмешательства.
> 4. **Уровни.** Пройди все `log.debug` и оцени частоту: сколько строк даст
>    эта точка за час игры. Всё, что реже ~10 раз за игру и означает РЕШЕНИЕ,
>    обязано быть `info`. Всё, что чаще, — обязано остаться `debug`.
> 5. **Шум.** Обратная задача: посчитай объём лога за час игры при нынешних
>    уровнях (буфер кольцевой — что вытесняется первым?). Есть ли точки,
>    забивающие буфер так, что начало сессии теряется.
> 6. **Опознаваемость.** Хватает ли в строке контекста (какая вкладка, какая
>    фаза, какое значение), чтобы отличить причину от следствия. Русский текст
>    предпочтительнее: лог читает владелец, а не только агент.
> 7. **Секреты.** Любая новая info-строка не должна тащить в файл `authKey`,
>    пароль OBS, токен твича (см. `redactSecrets`).
> 8. **Ответ пользователю.** Где расширение молча не сработало, но могло бы
>    честно сказать человеку (тост/панель), — отдельным списком.
>
> Формат: находки, сгруппированные по фичам; каждая — SEVERITY
> (КРИТИЧНО = типовая жалоба неразбираема / ВАЖНО / МЕЛОЧЬ), файл:строка,
> сценарий пользователя одной фразой, чего не хватает в логе, предлагаемая
> строка и уровень. В конце: (1) таблица «типовая жалоба → строки лога,
> которые её закрывают», (2) оценка объёма лога за час игры до и после,
> (3) список направлений, где всё чисто.

## Перф-аудит: что замедляет расширение и сайт во время игры (проверен 01.08.2026)

Результат первого прогона: 13 находок (1 критичная — canvas-переворот камеры,
7 важных), все подтверждены adversarial-ревью, применены в 9.0.0. Отчёт:
docs/audit-findings-2026-08-01.md.

Промт (отдавать свежему агенту как есть; перед запуском убедиться, что пути
и известная история в секции КОНТЕКСТ актуальны):

> Ты — аудитор производительности браузерного расширения Polemica Notes
> (Chrome/Firefox MV3, TypeScript). ЗАДАЧА: найти всё, что может замедлять
> расширение и — главное — сайт polemicagame.com во время игры. Итог — ОТЧЁТ,
> код НЕ менять: находки пойдут на adversarial review, каждая должна стоять
> на доказательствах. Перед началом прочитай AGENTS.md §3, §4, §2а.
> Направления атаки (по каждому — вердикт «чисто» или находка):
> 1. Layout thrashing (getComputedStyle/getBoundingClientRect в DOM-проходах).
> 2. Пробуждение наблюдателя нашими же записями (неидемпотентные записи).
> 3. Селекторная стоимость (широкие QSA × частота × размер DOM).
> 4. Таймеры: полный список интервалов, что живёт одновременно в комнате,
>    что не снимается в disable().
> 5. Storage-каналы: частота get/set в горячих путях, сериализация больших
>    карт, логгер.
> 6. Память/утечки: Map vs WeakMap, отписки, детачнутые узлы.
> 7. Сеть в горячих путях: bursts, кэш, дедупликация, таймауты.
> 8. Стартовая стоимость content.js (parse/compile до первого кадра).
> Формат: находки по влиянию на игровую комнату; каждая — SEVERITY
> (КРИТИЧНО/ВАЖНО/МЕЛОЧЬ), файл:строка, механизм с арифметикой
> (частота × стоимость), цитата кода, фикс в 1-2 фразах + задетые
> инварианты §4. В конце: топ-3 и список чистых направлений. Без
> микрооптимизаций без измеримого эффекта.
