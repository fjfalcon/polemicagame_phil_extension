# Леджер adversarial-волн

Механический след контура ревью (решение владельца 26.08.2026): каждая волна
— строка. `release:assets` мягко предупреждает, если текущая версия здесь не
упомянута. Канарейка — по промту №12; мета-ревью — №11 (раз в ~5 релизов).

| Версия | Дата | Промты/агенты | Находки | Канарейка |
|---|---|---|---|---|
| 9.28.0 | 16.08.2026 | №7 (одиночный) | без блокеров; ПРОПУЩЕН класс «маршрут пользователя не проигран» (showPanel-гейт «чата везде») — всплыл жалобой | не проводилась |
| 9.32.1 | 26.08.2026 | №7 ×4 специализированных агента | 2 блокера (DOM-цикл карточек §4; осиротение записи OBS), 4 высоких, ~20 прочих — все закрыты до стора | не проводилась |
| 9.33.1 | 26.08.2026 | — (инфраструктура контура) | fixpoint-харнес мутационно проверен откатом hiddenFor (3 красных) | аналог: канарейка внутри харнеса |
| 9.34.0 | 26.08.2026 | №7 (одиночный, по волне гейта/privacy/enrollment) | без блокеров; 5 средних: обход механики зачисления (комментарий/vi.mock), 4 неточные причины EXEMPT, tileLabel — нестабильный DOM-индекс вместо родного номера места; всё закрыто второй итерацией до пуша | не проводилась |

| 9.34.x | 26.08.2026 | №11 (мета-ревью, ПЕРВЫЙ прогон) | 3 системных слепых зоны: (A) маршрут обещания фичи не проигрывается — №7з+№10-сценарий; (B) попап вне области всех промтов, предикаты читателей ключа не сверялись — №8п6+№2п8; (C) инструментальный контур не аудировался, «проверка, чей результат никто не читает» — №5п4+№12п5. 9 правок промтов применены; сокращены выродившиеся пункты №5 (CSP-свип, WAR, sender) и дубль __proto__ в №2. Мета-баг процесса: «чат везде» числился закрытым пропуском без строки-механизма | — |
| 9.35.0 | 26.08.2026 | №7 (одиночный, по волне publish-гейта/no-nicks/enrollment) | 1 высокая: redactNick в сигнатуре кнопок ослабил детекцию смены игрока (риск «заметка не тому») — редакция перенесена в точку лога; 1 средняя: цифры согласия фолбэк-импорта по старому снимку — свежий пересчёт + повторное согласие; 4 низких (страж-грep, verify-dist, пины) — закрыты | не проводилась |
| 9.36.0 | 26.08.2026 | внешнее ревью остаточных рисков + фиксы | gate-stamp (same-version stale закрыт: HEAD в штампе), петля фолбэка импорта вынесена в import-fallback.ts под 6 поведенческих тестов (перечитывание ПОСЛЕ каждого согласия; ok:false живого координатора больше не уходит в прямую запись), reason из мира страницы — белый список, причины EXEMPT nick-plate/postgame-search сверены с кодом, формулировка redactNick честная («слабая псевдонимизация») | не проводилась |
| 9.37.1 | 26.08.2026 | №7 (одиночный, по волне gate-stamp/unstable/privacy-тестов) | без блокеров; 2 средне-низких в treeSha256 (неинъективный фрейминг без длин — экспериментально подтверждённая коллизия; симлинки через statSync) и кривое сообщение о штампе старого формата — закрыты той же волной; порядок «штамп до гейта / после упаковки / подпись не трогает dist/firefox» проверен; живой прогон дополнительно поймал .amo-upload-uuid от web-ext sign внутри dist/firefox (агент утверждал обратное) — метаданные исключены из дайджеста | не проводилась |
| 9.38.0 | 26.08.2026 | №7 (одиночный, пятая волна: штамп-в-начало, consent-протокол координатора, строгий union) | закрыты: stale-штамп при падении readVersion; malformed-ответ координатора ({}, ok:"false") читался успехом; предел согласия теперь едет в notes_merge (consent_exceeded → новый вопрос, >2 — отмена); хэши финальных firefox-zip/XPI в штампе; леджер-строка 9.37.1 нормализована. Итерация №2 по adversarial: петля согласия вынесена в import-fallback.ts (общий MAX_CONFIRMS, 4 теста), malformed-ответ не портит цифры тоста, фолбэк стартует с итогового approved, approvedReplaced валидируется (NaN/отрицательные) | не проводилась |
| 9.39.0 | 26.08.2026 | шестая (финальная до боевого сигнала) волна | fail-closed consent-IPC: предел ОБЯЗАТЕЛЕН (bad_request), строгий payload consent_exceeded/success (классификатор classifyMergeResponse под тестами), фолбэк только на мёртвый фон — malformed = отказ, не прямая запись; release:check (все хэши штампа перед gh release), без XPI команда релиза не печатается; обрыв тела в contract-тестах — skip, не drift. КОНТУР ЗАМОРОЖЕН до боевого сигнала; плановое: мета-ревью №11 №2 | не проводилась |
| 9.39.0-perf | 26.08.2026 | перф-аудит (6 срезов, внешний агент; docs/audit-performance-2026-08-26.md) | 18 пунктов: 2 high (WS-log учёт/backpressure делит квоту с заметками — подтверждено вручную по ws-log.ts; исполняемость лишь 4/18 старых бюджетов), 11 medium (прогрев кроссовера, route-жизнь фич, бандл +34.7%, дубль OBS-reconcile…), 5 low. Код не менялся; план фиксов — по приоритетам отчёта | — |
| 9.40.0 | 26.08.2026 | перф-волна по плану аудита 26.08 (п.1–4) | PERF26-4 закрыт (единый учёт после sweep, backpressure ЦЕПОЧКИ записи, потолок ключей, поколение сессии — 4 теста); PERF26-3 (прогрев мелкой страницей limit=200 + общий кэш своей истории в @core/crossover — было три); PERF26-8 (never-overlap /api/games: TTL от развязки, identity-гейт, AbortSignal — 2 поведенческих теста); PERF26-12 (дедуп сверки на пробуждение для top-level/onInstalled/alarm; onStartup остаётся гарантированным осознанно); PERF26-17 (эхо собственной смены сцены не рассылается дважды); PERF26-6 частично (пины горячих констант + бюджеты). Итерация №2 по adversarial (2 HIGH!): finishSession не теряет хвост при выключении лога; force-restore для onInstalled (protocol-блок оставался навсегда); маркер отброса — В КУСКАХ (счётчик попапу недоступен); release своей истории убран из горячего прохода/disable (выбивал кэж из-под ховера и карточек — контракт wiring-теста сменён); sweep не трогает свою сессию; гард поздней развязки id; эхо сцены гасится и в setCurrentScene | — |
| 9.41.0-sec | 26.08.2026 | security-аудит (внешний агент; docs/audit-security-2026-08-26.md) | 4 high (obs_host с кредами → sync+бэкап — подтверждено: LOCAL_KEYS только пароль; учёт ws-log в символах тела vs ~4.8x JSON; завершённые поколения сессии вне sweep и лимитов; RMW-гонка миграции заметок мимо координатора — подтверждено по notes-store), 6 medium, 4 low, 3 process. Код не менялся; план — по приоритетам | — |
| 9.42.0 | 27.08.2026 | security-волна по аудиту 26.08 (4 high + 4 medium + REL26-2) | SEC26-1 (sanitizeObsHost при сохранении + разовая нормализация sync); SEC26-3 (учёт ws-лога сериализованным размером); SEC26-4 (sweep исключает только активное поколение; enable ждёт закрытия прошлой сессии); SEC26-5 (запись миграции заметок — только координатор, контенты получают вид в памяти + notes_migrate); SEC26-8/2 (OPERATIONAL_KEYS += skip_start/автозапись/клипы/полный лог); SEC26-9 (очистка ws-лога добивает контент-контексты); SEC26-6 (агрегатные потолки импорта roleMarks); REL26-2 (release:check в команде). Итерация №2 по adversarial (HIGH: импорт-путь obs_host мимо санитайзера; enable ws-лога слушал до закрытия прошлой сессии): санитайзер на импорт-патче + поле показывает очищенное; enable ждёт startSession до подписки; метрики потолков сведены к сериализованной (PENDING 1М, MAX_TOTAL задокументирован честно); retry просьбы миграции жив (ответ ok, не мёртвый catch); порядок очистки лога reset→диск; roleMarks: тост о срезке, пустые игры не занимают слот; sanitizeObsHost сохраняет путь и не портит bare-host | — |
| 9.43.0 | 27.08.2026 | ревью «обещание шире границы» (3 пункта + мелочи) | obs_host санитизируется НА ГРАНИЦЕ settings (setSettings + getSettings): новый writer/грязный sync/коррупция больше не выносят креды, call-site дубли убраны; WS-лог: clearAll возвращает результат (тост не врёт), finishSession дописывает маркер недоехавшего дропа; extras: провал импорта виден в тосте, потолок игр roleMarks стал ТОТАЛЬНЫМ (50 после слияния, а не +50); contract /api/games проверяет обещанный mmr; пин числа подписок для fixpoint-covered. Отложено сознательно: автономный TTL-reclaim ws-лога, полная сериализация extras через координатор (SEC26-7), baseline-after-disable в fixpoint. Итерация №2 по adversarial (5 средних в моих же фиксах): потолок меток снова пропускает импорт (подрезка ОБЩЕГО результата, как runtime — «итог>=50» вливал НОЛЬ активному игроку); файл с нулём кадров, но дропом больше не зовётся «пустым»; фон нормализует патч ДО сравнения интентов (грязный sync снимал ручное отключение и persisted-блок) и чинит диск сразу; сравнение адресов при импорте — нормализованное (косметика «/» стирала пароль); reflectPatch не рисует креды; маркер дропа не пишется при мёртвом хранилище и не вытесняет реальный кусок; инвариант «obs_host только через границу»; гигиена моков settings.test | — |
| 9.44.0 | 27.08.2026 | живые замеры скорости (внешний агент) + реализация 6 рекомендаций | история одним запросом limit=8000 (−45%: 2.6–2.85 с → 1.41–1.48 с при том же трафике, capped честен); мгновенный показ готового кэша (пересечения и последние игры) вместо фиксированных 350 мс; shallow-сводка прогрева показывается сразу, точная докачивается после hover-intent; последние игры рисуются до разборов, «ПУ» дорисовывается прогрессивно; строки ночного прогрева переиспользуются как кэш последних игр. Тест поймал баг порядка (кэш-хит красил в скрытый тултип) | — |
| 9.45.0 | 27.08.2026 | ревью «обещание шире механики» (3 п.) | п.1 (ПОТЕРЯ ДАННЫХ): round-trip своего бэкапа резал заметку 20000→5000 молча — импорт получил потолок MAX_OWN_NOTE_TEXT, mergeNotes считает truncated, тост честный; п.2: смена OBS-endpoint коалесцируется 200 мс (host в sync и пароль в local приходили порознь → коннект к новому адресу со старым паролем), события отдают санитизированный host, ручной connect тоже; п.3: единый признак неполноты ws-лога (backpressure + вытеснение + отказ записи + остановленный лог), ошибка ЧТЕНИЯ ≠ «лог не включали». Итерация №2 по adversarial (15 находок, 1 HIGH): фолбэк импорта резал СВОЮ заметку до 5000 и врал в тосте — тот же потолок + truncated с фактического пути; персистентный признак неполноты ws-лога (WS_LOSS_KEY: stopped/вытеснение/sweep больше не молчат); счётчик не считает медиа-кадры и учитывает только ПРИМЕНЁННЫЕ обрезки; пропуск негодных записей показывается отдельно; миграция sync-моста своим потолком; коалесценция OBS покрыта тестами (блок не покрывался ВООБЩЕ) + строка «переход настроек OBS» в журнале; сравнение prev/next по санитизированным. Принято: раздельные change-события host/пароль с интервалом в секунды (коалесценция окном 200 мс их не склеит), 20000-символьный потолок применяется к любому файлу | — |
| 9.46.0 | 27.08.2026 | жалоба игрока «Max MMR неправильный» | подпись «макс» стояла под максимумом ОКНА графика (120 игр): у игрока с 3583 играми карьерный пик 9171 (30.04.2026) против показанных 8972. График остался окном (линия по тысячам игр нечитаема), но ЧИСЛА считаются по полной истории (общий кэш, один запрос) и подписи говорят прямо: «график: последние 120 игр» / «макс … (за всё время, N игр)»; при упоре в лимит выдачи — «за доступный отрезок» | — |
| 9.47.0 | 27.08.2026 | локальный аудит 9.46.0 (8.2/10, «три runtime-обхода живы») | БЛОКЕР закрыт: пара «адрес+пароль» OBS едет одной ТРАНЗАКЦИЕЙ сообщением (разные области хранилища + правки с интервалом в секунды таймером не склеиваются), тест с разрывом 5 с; п.1: maxLength на всех textarea, прямой фолбэк нормализует карту, координатор возвращает truncated/skipped, небезопасный ключ считается пропуском; п.3: ОДИН авторитетный счётчик потерь (dropped из кусков убран — был двойной счёт), запись сериализована, попап просит вкладки дописать хвост перед выгрузкой; package-lock 9.1.0→версия релиза + страж в гейте; мок storage в тестах научился читать по ключам (прежний всегда отдавал {} и скрывал бы чтение-зависимые пути). Принято: raw external text в логе, extras без координатора, полный fixpoint, dirty-warning, CWS uploadState, подписанная attestation | — |
| 9.48.0 | 27.08.2026 | проход по 9.47.0 («границы не закрыты до конца») | п.1: счётчики truncated/skipped доходят до UI обоими путями (координатор пробрасывает, попап берёт авторитетный, commitNoteOps говорит «сохранена не полностью»), фолбэк нормализует ТОЛЬКО затронутые записи (чужая длинная больше не режется), isSafeNoteKey в notes_apply_ops; п.2: смена host сразу обнуляет пароль, транзакция применяется ИЗ PAYLOAD (без повторного чтения расщеплённого хранилища), отказ виден пользователю, ручной connect ждёт сохранения, импорт бэкапа тоже шлёт транзакцию; п.3: settleAll — полный барьер (куски+счётчик), слушатель жив до конца finishSession, поколение гейтит отложенный RMW счётчика, потеря отмечается после успешного remove, legacy chunk.dropped не складывается, у счётчика TTL, молчание вкладки видно в тосте | — |
| 9.49.0 | 27.08.2026 | проход по 9.48.0 (стоп-фактор: частичная запись storage) | п.1: ТРАНЗАКЦИЯ владеет обеими записями (пароль в local → адрес в sync, последовательно; упал пароль — адрес не пишем вовсе, отказ со stage), попап больше не пишет пару сам, undefined/{ok:false} считаются ошибкой в save/import/manual, saveSettings не глотает ошибку; п.2: generation перечитывается ПОСЛЕ await, таймаут 1.5 с на вкладку, попап ждёт loss от sweep, TTL-уборка не считается потерей (только бюджет), numeric-счётчик мигрирует в формат с меткой, legacy chunk.dropped учитывается только при отсутствии нового счётчика; п.3: counters обязательны при ok:true (classify требует их), нестроковый ключ считается пропуском, моки заменены НАСТОЯЩИМИ функциями, добавлен тест «фолбэк не трогает чужую длинную запись» | — |
| 9.49.1 | 27.08.2026 | добивка по 9.49.0 (импорт мимо транзакции) | импорт бэкапа больше НЕ везёт obs_host/obs_password общим setSettings — только транзакцией (частичный отказ local при успешном sync давал расщеплённую пару до всякой транзакции); отказ транзакции = reject, ручной connect не едет; адрес, приехавший ПО SYNC с другого устройства, не тащит локальный старый пароль — рвём связь и ждём ввода (строка в журнале); finishSession дожидается учёта потерь ДО resetBuffer; ответ координатора ok:true без счётчиков помечается в журнале; тест фолбэка гоняет РЕАЛЬНУЮ normalizeTouched, sync-мок в OBS-тестах stateful (пустой делал проверку «адрес не записан» ложно-зелёной) | — |
| 9.49.2 | 27.08.2026 | последний стоп-фактор OBS | адрес с другого устройства без смены пароля: пароль прежнего сервера СТИРАЕТСЯ, ставится дисковый флаг obs_awaiting_password, reconcile его уважает — запрет переживает рестарт service worker (прежний разрыв связи забывался, и restore уходил на новый адрес со старым паролем); транзакция снимает флаг при вводе пароля здесь. Тест: sync-host → рестарт → подключения нет → ввод пароля → подключение | — |
| 9.50.0 | 27.08.2026 | adversarial по всему v9.32.1..HEAD (4 агента) | 4 БЛОКЕРА: импорт бэкапа без пароля стирал сохранённый пароль OBS; дисковый флаг obs_awaiting_password запирал OBS навсегда, если пароль не вводили здесь; ночной прогрев засевал кэш «последних игр» готовым списком БЕЗ разборов — «ПУ» молча пропадал на время жизни кэша; полная история соперника качалась дважды (2000, затем 8000). Модель кредов OBS переписана с флага на ПРИВЯЗКУ пароля к адресу (obs_password_host в local, сверяется перед каждым коннектом; пароль не стирается, просто не применяется к чужому адресу), транзакция обязана обходиться без пароля и больше не коннектит сама (reconcile), get_status отдаёт needsPassword, попап показывает «нужен пароль для нового адреса»; countRoomTabs c таймаутом 1.5 с на вкладку. Заметки: миграция из облака своим потолком и с журналом потерь, удаление обрабатывается ДО строгого фильтра ключей, предупреждение о срезке только при успешной записи. Контент: кэши пересечений/графика ключуются своим аккаунтом, тултип-призрак, lastGamesProgress чистится, полная история — один запрос. Новое: тумблер «клик по нику переключает сворачивание». 2 стража добавлены мутационной проверкой (откат фикса → красный) | не проводилась |
| 9.50.1 | 27.08.2026 | жалоба стримера «V показывает все роли, кроме моей» | «подсмотреть» снимало ТОЛЬКО css-слой, а своя роль скрыта ещё и нативно (#stop) и/или inline — на экране появлялись роли всех, кроме собственной. startPeek снимает все три слоя (как handleRoleKey и ночной автопоказ), stopPeek возвращает каждый снятый слой; нативное скрытие возвращается ВСЕГДА, без оглядки на настройки и фазу, с проверкой и ретраями (роль, оставшаяся на экране, уезжает в эфир); таймер возврата гасится в disable(). Страж проверен мутацией |  |
| 9.51.0 | 27.08.2026 | просьба владельца: «Мой вечер» — настройки вида как у чата + системные строки чата | общий модуль @core/panel-chrome (санитайзер недоверенного localStorage страницы, применение фона/заголовка/сквозь-кликов, элементы меню): twitch-панель переведена на него, «Мой вечер» получил тот же набор — фон 0–100%, скрытие заголовка с полоской-ручкой, шрифт S/M/L, число игр в списке, «сквозь клики» с чипом-замком. Ресайз: три ручки (правый край / нижний край / видимый уголок) вместо одного невидимого угла 14×14, потолок по вьюпорту, кламп панели при уменьшении окна БЕЗ записи подрезанной коробки на диск. 11 поведенческих тестов + property-тесты схемы; три мутанта убиты (край меняет обе стороны, rowsLimit игнорируется, кламп пишет на диск). Плюс жалоба по скриншоту: «подключаемся» и «подключились» занимали две строки — исход попытки теперь переписывает СВОЮ строку (ключ обновляемой системной строки, обновляется только последняя), разные события остаются раздельными; 4 теста, 2 мутанта убиты | не проводилась |
| 9.51.0-adv | 27.08.2026 | adversarial волна по 9.51.0 (промты №7/№10/№8/№4, 4 агента) | БЛОКЕР (найден тремя независимо): clampToViewport на СКРЫТОЙ панели читал нулевой rect и писал минимальный размер в угол — стример прятал панель, менял размер окна и терял коробку; кламп теперь не трогает display:none и нулевой rect, а показ панели её поправляет. Высокие: перетаскивание не клампилось вовсе (панель утаскивалась за экран без возврата до F5); ресайз не нормализовал якорь — свежая панель на right:16px росла ВЛЕВО из-под курсора; подрезанный клампом размер закреплялся на диске первым же сдвигом (введена desiredBox: «подвинул» сохраняет положение и ПРЕЖНИЙ размер); попап писал «Подключено» из onopen, пока панель говорила «канал не отвечает» — статус теперь по подтверждённому входу (ircReady); лестница переподключений давала до 20 системных строк (у «Отключились» не было ключа склейки; текст «отключились» врал, когда подключения не было). Средние: box-sizing корня (rect≠style → рост на 2px за каждое событие resize); ручки ресайза крали скроллбар тела и правый край шапки; насечка уголка светилась на прозрачном оверлее «сквозь клики»; скрытый заголовок «Моего вечера» отбирал ⟳/↺/× — полоска теперь несёт их; «фон 0%» не убирал подложку сводки; сводка трёхчасовой давности выглядела свежей (строка «обновлено N назад» + обновление на возврат вкладки); попап рисовал twitch-тумблер truthy-предикатом (класс жалобы «кнопок нет»). Низкие: дефолт панели в санитайзере игнорировался для 3 полей, потолок сырого JSON, осиротевшие текстовые узлы в окне чата, безусловные записи style в applyChrome/полоске (ползунок «Фон» будил наблюдатель ~150 раз за жест), touch-action/user-select. 7 новых стражей, каждый проверен мутацией. ОТЛОЖЕНО в техдолг: ложные срабатывания сторожа шторма от собственного рендера чата вне комнаты; форсированный layout на каждое сообщение (scrollHeight); пробуждение подписчиков 4 раза/с на время перетаскивания панели (дроссель 250 мс держит); уборка legacy-ключей twitch-chat-position/size из localStorage сайта | не проводилась |

Примечание к гейту: «полная сюита» в release:assets = вся ОФЛАЙН-сюита
(vitest run: unit + invariants). test:contract исключён из гейта ОСОЗНАННО:
он ходит в живой сайт, и релиз не должен блокироваться чужим даунтаймом —
контракт-сюита гоняется по требованию и перед волнами (§2в AGENTS).
